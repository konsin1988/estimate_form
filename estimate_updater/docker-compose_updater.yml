services:
  revenue_monthly_updater:
    env_file: 
      - "../.env"
    build:
      context: .
      dockerfile: Dockerfile
    image: konsin1988/estimate-updater:latest
    container_name: estimate-updater 
    environment:
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_HOST=${DB_HOST}
      - DB_PORT=${DB_PORT}
      - DB_NAME=${DB_NAME}
    networks:
      - revenue-estimate-network

networks:
  revenue-estimate-network:
    name: estimate-network
    external: True
